export type Product = {
  name: string;
}

export type Pokemon = {
  name: string;
  sprite: string;
  price: number;
  amount: number;
}

export type User = {
  name: string;
  email: string;
  password: string;
}

